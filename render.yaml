services:
  - type: web
    name: n8n-websocket-server
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node deploy-railway.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
